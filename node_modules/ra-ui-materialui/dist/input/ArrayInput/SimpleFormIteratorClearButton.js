import * as React from 'react';
import { useState } from 'react';
import { useEvent, useSimpleFormIterator, useTranslate, } from 'ra-core';
import { Confirm } from "../../layout/Confirm.js";
import { ClearArrayButton } from "./ClearArrayButton.js";
export var SimpleFormIteratorClearButton = function (_a) {
    var className = _a.className, disableClear = _a.disableClear, disableRemove = _a.disableRemove;
    var translate = useTranslate();
    var _b = useState(false), confirmIsOpen = _b[0], setConfirmIsOpen = _b[1];
    var _c = useSimpleFormIterator(), clear = _c.clear, total = _c.total;
    var handleArrayClear = useEvent(function () {
        clear();
        setConfirmIsOpen(false);
    });
    if (total === 0 || disableClear === true || disableRemove === true) {
        return null;
    }
    return (React.createElement(React.Fragment, null,
        React.createElement(Confirm, { isOpen: confirmIsOpen, title: translate('ra.action.clear_array_input'), content: translate('ra.message.clear_array_input'), onConfirm: handleArrayClear, onClose: function () { return setConfirmIsOpen(false); } }),
        React.createElement(ClearArrayButton, { className: className, onClick: function () { return setConfirmIsOpen(true); } })));
};
//# sourceMappingURL=SimpleFormIteratorClearButton.js.map