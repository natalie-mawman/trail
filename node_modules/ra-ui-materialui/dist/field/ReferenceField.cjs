"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ReferenceFieldClasses = exports.ReferenceFieldView = exports.ReferenceField = void 0;
var React = __importStar(require("react"));
var material_1 = require("@mui/material");
var styles_1 = require("@mui/material/styles");
var Error_1 = __importDefault(require("@mui/icons-material/Error"));
var ra_core_1 = require("ra-core");
var clsx_1 = __importDefault(require("clsx"));
var layout_1 = require("../layout/index.cjs");
var Link_1 = require("../Link.cjs");
var genericMemo_1 = require("./genericMemo.cjs");
var utils_1 = require("@mui/utils");
var Offline_1 = require("../Offline.cjs");
/**
 * Fetch reference record, and render its representation, or delegate rendering to child component.
 *
 * The reference prop should be the name of one of the <Resource> components
 * added as <Admin> child.
 *
 * @example // using recordRepresentation
 * <ReferenceField label="User" source="userId" reference="users" />
 *
 * @example // using a Field component to represent the record
 * <ReferenceField label="User" source="userId" reference="users">
 *     <TextField source="name" />
 * </ReferenceField>
 *
 * @example // using a render prop to render the record
 * <ReferenceField label="User" source="userId" reference="users" render={
 *     (context) => (
 *         <p>{context.referenceRecord?.name}</p>
 *     )
 * }>
 * </ReferenceField>
 *
 * @example // By default, includes a link to the <Edit> page of the related record
 * // (`/users/:userId` in the previous example).
 * // Set the `link` prop to "show" to link to the <Show> page instead.
 * <ReferenceField label="User" source="userId" reference="users" link="show" />
 *
 * @example // You can also prevent `<ReferenceField>` from adding link to children
 * // by setting `link` to false.
 * <ReferenceField label="User" source="userId" reference="users" link={false} />
 *
 * @example // Alternatively, you can also pass a custom function to `link`.
 * // It must take reference and record as arguments and return a string
 * <ReferenceField label="User" source="userId" reference="users" link={(record, reference) => "/path/to/${reference}/${record}"} />
 *
 * @default
 * In previous versions of React-Admin, the prop `linkType` was used. It is now deprecated and replaced with `link`. However
 * backward-compatibility is still kept
 */
var ReferenceField = function (inProps) {
    var props = (0, styles_1.useThemeProps)({
        props: inProps,
        name: PREFIX,
    });
    var children = props.children, render = props.render, emptyText = props.emptyText, empty = props.empty, _a = props.offline, offline = _a === void 0 ? defaultOffline : _a, rest = __rest(props, ["children", "render", "emptyText", "empty", "offline"]);
    var translate = (0, ra_core_1.useTranslate)();
    return (React.createElement(ra_core_1.ReferenceFieldBase, __assign({}, rest, { empty: emptyText ? (React.createElement(material_1.Typography, { component: "span", variant: "body2" }, emptyText && translate(emptyText, { _: emptyText }))) : typeof empty === 'string' ? (React.createElement(material_1.Typography, { component: "span", variant: "body2" }, empty && translate(empty, { _: empty }))) : (empty !== null && empty !== void 0 ? empty : null), offline: offline }),
        React.createElement(PureReferenceFieldView, __assign({}, rest, { render: render }), children)));
};
exports.ReferenceField = ReferenceField;
var defaultOffline = React.createElement(Offline_1.Offline, { variant: "inline" });
// useful to prevent click bubbling in a datagrid with rowClick
var stopPropagation = function (e) { return e.stopPropagation(); };
var ReferenceFieldView = function (props) {
    var _a = (0, styles_1.useThemeProps)({
        props: props,
        name: PREFIX,
    }), children = _a.children, render = _a.render, className = _a.className, emptyText = _a.emptyText, reference = _a.reference, sx = _a.sx, rest = __rest(_a, ["children", "render", "className", "emptyText", "reference", "sx"]);
    var referenceFieldContext = (0, ra_core_1.useReferenceFieldContext)();
    var error = referenceFieldContext.error, link = referenceFieldContext.link, isLoading = referenceFieldContext.isLoading, referenceRecord = referenceFieldContext.referenceRecord;
    var getRecordRepresentation = (0, ra_core_1.useGetRecordRepresentation)(reference);
    if (error) {
        return (React.createElement("div", null,
            React.createElement(Error_1.default, { role: "presentation", color: "error", fontSize: "small" }),
            React.createElement("span", { style: utils_1.visuallyHidden }, typeof error === 'string' ? error : error === null || error === void 0 ? void 0 : error.message)));
    }
    // We explicitly check isLoading here as the record may not have an id for the reference,
    // in which case, the query will not be enabled and isPending will be true
    // isLoading checks that we are actually loading the reference record
    if (isLoading) {
        return React.createElement(layout_1.LinearProgress, null);
    }
    var child = (render ? render(referenceFieldContext) : children) || (React.createElement(material_1.Typography, { component: "span", variant: "body2" }, getRecordRepresentation(referenceRecord)));
    if (link) {
        return (React.createElement(Root, __assign({ className: (0, clsx_1.default)(exports.ReferenceFieldClasses.root, className), sx: sx }, rest),
            React.createElement(Link_1.Link, { to: link, className: exports.ReferenceFieldClasses.link, onClick: stopPropagation, state: { _scrollToTop: true } }, child)));
    }
    return (React.createElement(Root, __assign({ className: (0, clsx_1.default)(exports.ReferenceFieldClasses.root, className), sx: sx }, rest), child));
};
exports.ReferenceFieldView = ReferenceFieldView;
var PureReferenceFieldView = (0, genericMemo_1.genericMemo)(exports.ReferenceFieldView);
var PREFIX = 'RaReferenceField';
exports.ReferenceFieldClasses = {
    root: "".concat(PREFIX, "-root"),
    link: "".concat(PREFIX, "-link"),
};
var Root = (0, styles_1.styled)('span', {
    name: PREFIX,
    overridesResolver: function (props, styles) {
        var _a;
        return (_a = {},
            _a['&'] = styles.root,
            _a["& .".concat(exports.ReferenceFieldClasses.link)] = styles.link,
            _a);
    },
})(function (_a) {
    var _b;
    var theme = _a.theme;
    return (_b = {
            lineHeight: 'initial'
        },
        _b["& .".concat(exports.ReferenceFieldClasses.link)] = {
            '& > *': {
                color: (theme.vars || theme).palette.primary.main,
            },
        },
        _b);
});
//# sourceMappingURL=ReferenceField.js.map